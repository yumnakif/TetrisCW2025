package com.comp2042.input;

/**
 * Enum representing the type of game event
 * For categorizing between the various actions that can be performed on the current brick
 */
public enum EventType {
    DOWN, LEFT, RIGHT, HARD_DROP, ROTATE
}
